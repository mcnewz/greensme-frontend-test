'use client'
import React from 'react'

const ProductCard = () => {
  return (
    <dev>
        <button onClick={() => console.log("Add")}> Add to cart</button>
    </dev>
  )
}

export default ProductCard